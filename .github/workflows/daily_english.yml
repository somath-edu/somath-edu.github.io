name: Daily English Worksheet
on:
  schedule:
    - cron: '0 23 * * *' # 한국 시간 매일 오전 8시 (UTC 23:00)
  workflow_dispatch: # 필요할 때 수동으로 버튼 눌러 실행 가능

jobs:
  publish_worksheet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - run: pip install google-generativeai

      - name: AI 직독직해 연습장 발행
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python .github/scripts/daily_english.py

      - name: 변경사항 저장 및 푸시
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add .
          git commit -m "docs: 📝 오전 8시 직독직해 연습장 정기 발행" || echo "No changes to commit"
          git push
