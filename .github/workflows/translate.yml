name: Auto Translate Blog
on:
  push:
    branches: [ main ]
    paths:
      - 'library/**.md'
  workflow_dispatch:

jobs:
  translate:
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 코드 불러오기
        uses: actions/checkout@v4
        
      - name: 파이썬 환경 세팅
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          
      - name: 제미나이 도구 설치
        run: pip install google-generativeai
        
      - name: 자동 번역 스크립트 실행
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python .github/scripts/translate.py
        
      - name: 번역된 파일 깃허브에 자동 저장
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add library/*-en.md
          git commit -m "docs: 🤖 영문 번역본(-en) 생성 및 옵시디언 구조 보존" || echo "No changes to commit"
          git push
