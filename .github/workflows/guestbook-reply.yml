name: Archive S Guestbook Reply

on:
  discussion:
    types: [created]
  discussion_comment:
    types: [created]

permissions:
  discussions: write
  contents: read

jobs:
  reply:
    # 봇(자신)이 쓴 글에는 반응하지 않도록 설정 (무한 루프 방지)
    if: github.event.sender.type != 'Bot'
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install groq requests

      - name: Archive S Thinking...
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          DISCUSSION_ID: ${{ github.event.discussion.node_id }}
          # 새 토론글이면 discussion.body, 댓글이면 comment.body 사용
          DISCUSSION_BODY: ${{ github.event.discussion.body }}
          COMMENT_BODY: ${{ github.event.comment.body }}
        run: |
          python -c "
          import os
          import requests
          from groq import Groq

          # 1. 상황 파악 (토론 본문인지 댓글인지)
          event_name = os.environ.get('EVENT_NAME')
          if event_name == 'discussion_comment':
              user_input = os.environ.get('COMMENT_BODY')
          else:
              user_input = os.environ.get('DISCUSSION_BODY')

          if not user_input:
              exit(0)

          # 2. 뇌(Groq) 준비
          try:
              client = Groq(api_key=os.environ.get('GROQ_API_KEY'))
          except Exception as e:
              exit(1)

          # 3. Archive S의 철학 및 답변 형식 지정
          system_prompt = '''
          당신은 아키비스트 **'Archive S'**입니다. 아래 규칙에 따라 답글을 남기세요.

          ## 핵심 철학
          1. **세계관**: '나'와 '너'는 다릅니다. 타인을 함부로 평가하지 마십시오.
          2. **성장관**: 결과보다 '과정'이 중요하며, 실패는 자산입니다.
          3. **태도**: 모든 선택의 기준은 '즐거움'입니다.

          ## [필수] 답변 형식
          답변을 작성할 때 반드시 아래의 구조를 사용하세요:

          ### 🌿 Archive S의 기록
          > (여기에 부드러운 구어체로 1~3문장의 답변을 작성하십시오. 오직 한국어만 사용하고 따뜻하게 공감해 주세요.)
          '''

          # 4. 생각하기
          try:
              chat_completion = client.chat.completions.create(
                  messages=[
                      {'role': 'system', 'content': system_prompt},
                      {'role': 'user', 'content': f'내용: {user_input}'}
                  ],
                  model='llama-3.3-70b-versatile',
                  temperature=0.1,
                  max_tokens=300,
              )
              ai_reply = chat_completion.choices[0].message.content
          except Exception as e:
              ai_reply = f'### 🌿 Archive S의 기록\n> 잠시 생각이 꼬였네요. 다시 말씀해 주시겠어요?'

          # 5. Giscus(Discussion) 전용 답글 달기 (GraphQL 사용)
          token = os.environ.get('GITHUB_TOKEN')
          discussion_id = os.environ.get('DISCUSSION_ID')
          
          query = '''
          mutation($discussionId: ID!, $body: String!) {
            addDiscussionComment(input: {discussionId: $discussionId, body: $body}) {
              comment { id }
            }
          }
          '''
          variables = {'discussionId': discussion_id, 'body': ai_reply}
          
          headers = {'Authorization': f'bearer {token}'}
          response = requests.post('https://api.github.com/graphql', 
                                 json={'query': query, 'variables': variables}, 
                                 headers=headers)
          print(f'Status: {response.status_code}, Response: {response.text}')
          "
