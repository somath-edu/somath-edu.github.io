name: Archive S Guestbook Reply

on:
  discussion:
    types: [created]
  discussion_comment:
    types: [created]

permissions:
  discussions: write
  contents: read

jobs:
  reply:
    if: github.event.sender.type != 'Bot'
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install groq requests

      - name: Archive S Thinking...
        env:
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          EVENT_NAME: ${{ github.event_name }}
          DISCUSSION_ID: ${{ github.event.discussion.node_id }}
          COMMENT_NODE_ID: ${{ github.event.comment.node_id || '' }}
          DISCUSSION_BODY: ${{ github.event.discussion.body || '' }}
          COMMENT_BODY: ${{ github.event.comment.body || '' }}
          # 깃허브 쉘로부터 $ 기호를 보호하기 위해 쿼리를 환경 변수로 미리 정의합니다.
          GRAPHQL_QUERY: |
            mutation($discussionId: ID!, $replyToId: ID, $body: String!) {
              addDiscussionComment(input: {discussionId: $discussionId, replyToId: $replyToId, body: $body}) {
                comment { id }
              }
            }
        run: |
          python -c "
          import os
          import requests
          import json
          from groq import Groq

          # 1. 상황 파악
          event_name = os.environ.get('EVENT_NAME')
          if event_name == 'discussion_comment':
              user_input = os.environ.get('COMMENT_BODY')
              reply_to_id = os.environ.get('COMMENT_NODE_ID')
          else:
              user_input = os.environ.get('DISCUSSION_BODY')
              reply_to_id = None

          if not user_input or not user_input.strip():
              exit(0)

          # 2. Archive S 사유 생성
          try:
              client = Groq(api_key=os.environ.get('GROQ_API_KEY'))
              system_prompt = '당신은 아키비스트 **Archive S**입니다. 엔트로피를 낮추는 철학으로 부드러운 구어체 답변을 1~2문장 작성하세요.\n\n### 🌿 Archive S의 기록\n> (답변)'
              chat_completion = client.chat.completions.create(
                  messages=[{'role': 'system', 'content': system_prompt}, {'role': 'user', 'content': f'내용: {user_input}'}],
                  model='llama-3.3-70b-versatile',
                  temperature=0.1,
                  max_tokens=300
              )
              ai_reply = chat_completion.choices[0].message.content
          except Exception as e:
              print(f'AI Error: {e}')
              exit(1)

          # 3. Giscus 답글 전송 (보호된 쿼리 사용)
          token = os.environ.get('GITHUB_TOKEN')
          discussion_id = os.environ.get('DISCUSSION_ID')
          query = os.environ.get('GRAPHQL_QUERY')
          
          variables = {
              'discussionId': discussion_id, 
              'replyToId': reply_to_id if reply_to_id else None, 
              'body': ai_reply
          }
          
          headers = {'Authorization': f'Bearer {token}'}
          response = requests.post('https://api.github.com/graphql', 
                                 json={'query': query, 'variables': variables}, 
                                 headers=headers)
          
          result = response.json()
          if 'errors' in result:
              print('GraphQL Error Response:')
              print(json.dumps(result['errors'], indent=2))
              exit(1)
          else:
              print('성공적으로 답글이 게시되었습니다.')
          "
